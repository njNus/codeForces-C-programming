#include <stdio.h>
int main(){
    int n;
    int i;
    int k;
    scanf("%d",&n);
    char arr[i];
    char ar1[k];
    for(int s=0;s<arr[i].length;s++){
        ar1[k]=CharAt (0) + length -2+ chatAt(string .length-1);
    }
  return 0;
}

