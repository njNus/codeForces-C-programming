#include<stdio.h>
int main(){
    int arr[]={2,3,4,10,40};
    int n=sizeof(arr);
    int x=10;
    int l,r,mid,result=-1;
    if(r>=l){
        mid=l+(r-l)/2;
    if(arr[mid]==x){
        return mid;
        }
        if(arr[mid]>x){
            return
        }
    }
    return 0;
}
